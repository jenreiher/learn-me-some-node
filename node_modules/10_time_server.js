// var net = require('net')

// var port = process.argv[2]

var date = function() getDateTime {
    year = date.getFullYear()  
    month = date.getMonth()     // starts at 0  
    day = date.getDate()      // returns the day of month  
    hour = date.getHours()  
    minute = date.getMinutes()
    var test = `${year}-${month}-${day} -${hour}:-${minute}`
    console.log('test');
  }

// var server = net.createServer(function (socket) {  
//   // socket.write(data);
  
//   socket.end(getDateTime());
// })  
// server.listen(3000);

